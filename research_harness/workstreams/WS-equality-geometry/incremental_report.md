# WS-equality-geometry incremental report

Initialized in co-mathematician migration at 2026-06-03T20:01:29+02:00.

## LOOP-0012 update (2026-06-03T20:20:58+02:00)

Artifact:

- `research_harness/adversarial_reviews/LOOP-0012/scalar_slack_equality_lane.md`

Concrete actions:

- Classified the coordinate `ExchangePenalty/GramSlack=1` equality cases into three support signatures: same row/column identical supports (`48`), same-row/same-column parallel disjoint translates (`144`), and diagonal identical supports (`72`).
- Compared this exact finite classification with the prior local zero-mode evidence for product-projection and diagonal-difference controls.

Current summary:

U-0003 remains unresolved.  The coordinate orbit/signature classification gives exact finite evidence about equality mechanisms but is not a global equality-family theorem on `Gr(2,16) x Gr(2,16)` and cannot be used as local-to-global proof.

Reviewer status: accepted_with_caveats / fail-closed.

## LOOP-0013 update (2026-06-03T21:10:12+02:00)

Artifact:

- `research_harness/adversarial_reviews/LOOP-0013/scalar_slack_equality_lane.md`

Concrete actions:

- Converted the three LOOP-0012 coordinate equality signatures into exact restricted parametrized families: same row/column identical support, same-row/same-column parallel translates, and diagonal identical support.
- Recorded exact constants: `GramSlack=ExchangePenalty=3/4`, `3`, and `15/4` respectively.

Current summary:

U-0003 is materially reduced but open. The equality signatures are no longer merely coordinate enumerations, but they are still restricted chart families and not a global equality classification on `Gr(2,16) x Gr(2,16)`.

Reviewer status: accepted_with_caveats / fail-closed.

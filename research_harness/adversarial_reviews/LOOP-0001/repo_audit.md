# LOOP-0001 Repo Audit: Kronecker-Sum SV / Rank-Two Partial-Trace Bridge

Scope: inspected the required artifacts in `.`, plus the relevant Lean file and current research-harness claim/brief. Claims below are based only on inspected contents.

## Status map

| Artifact path | Claim(s) found | Status | Evidence | Reusable content | Warnings / false routes | Recommended next loop input |
|---|---|---|---|---|---|---|
| `partial_trace_inequality_needed_for_sv_bound.tex` | Identifies the rank-two partial-trace inequality (PT) and proves its equivalence to the projection estimate onto the traceless Kronecker-sum subspace; derives the Kronecker-sum singular-value bound from PT and conversely derives PT/projection estimate from the SV bound. | Reusable lemma; incomplete for main theorem. | Lines 83-150 give explicit projection formula and norm identity; lines 155-188 give rank-two variational formula; lines 190-258 show PT/P equivalent to SV. The file explicitly presents PT as the “missing inequality” (lines 47-63, 260-287), not as proved. | Projection formula `P_S(C)`, centered partial-trace identity, rank-two variational formula, exact bridge between SV bound and PT. | Do not cite as proof of PT; it is a reduction/equivalence note. | Use as canonical derivation note. Next loop should attack PT or a strictly weaker projected version; preserve conventions for `tr_1`, `tr_2`, and rank as ordinary matrix rank. |
| `trace_inequality/rank_two_partial_trace_proof.tex` | Claims a complete rigorous proof of PT for rank <= 2. | False route / proof gap / obsolete-overstated. | Step 1 asserts ordinary matrix `rank(C) <= 2` gives a two-term operator-Schmidt decomposition `C=s1 A1⊗B1+s2 A2⊗B2` (lines 90-100). This confuses ordinary operator rank with operator-Schmidt rank and is false in general. Later scalar reduction claims `a+b <= 2+ab` for all `a,b in [0,4]` (lines 164-181), which is false, e.g. `a=4,b=0` gives `4 <= 2`. | The intended trace computations for a genuine operator-Schmidt rank <= 2 tensor may be reusable only under that different assumption. | Must not be promoted. Equality discussion and conclusion inherit the invalid reductions. | Build a skeptic note/counterexample to the Step 1 implication; if using Schmidt methods, switch to SVD/vector factorization `C=sum s_i |x_i><y_i|` as in the problem brief. |
| `problem_statement_aristotle/partial_trace_rank2_inequality.tex` | States PT and attempts proof via projection onto `S`; uses Kronecker-sum SV bound for every `W in S` to bound projection norm. | Incomplete/proof gap; circular if used for SV theorem. | Lines 58-82 correctly reduce PT to centered projection estimate. Lines 88-115 then invoke the Kronecker-sum bound `s1(W)^2+s2(W)^2 <= 1/2 ||W||_F^2` (lines 96-103) to prove PT. But `partial_trace_inequality_needed_for_sv_bound.tex` shows this SV bound is equivalent to PT, so this cannot serve as an independent proof of the bridge. | Centering identity and projection formula are reusable, consistent with the main bridge note. | Circular dependency: assumes the target theorem/missing bridge. | Replace assumed line 100-103 by an independent lemma or mark the file as conditional: “SV => PT”, not “PT proved”. |
| `problem_statement_aristotle/co_mathematician/COORDINATOR_REPORT_2026-05-09.md` | Workspace status report: theorem not proved; Frobenius identity proved in Lean; normal case proof exists; rank-two partial-trace inequality is missing bridge; numerical evidence finds no counterexample; main Lean theorem has `sorry`. | Accurate status summary / reusable coordination artifact. | Lines 5-8 say fail-closed and theorem not proved. Lines 12-18 list solid items. Lines 20-26 identify overstatement, `sorry`, false normal-density route, and failed polarization route. Lines 51-65 summarize numerical PT checks and equality witness. | Good roadmap and warnings. Sharpness witness and exact equality witness are reusable test cases. | Treat dated numerical claims as evidence only. It reports `lake build` not completed (lines 34-36). | Use report’s “What Is Not Solid” as guardrails. Next loop should not repeat normal-density or polarization route. |
| `problem_statement_aristotle/co_mathematician/workstreams/05_partial_trace_inequality.md` | Records numerical testing of PT, optimized gaps near zero, and explicit equality witness. | Numerical only; reusable equality witness. | Lines 24-29 specify the script and factorized optimization. Lines 33-48 record optimized gaps around negative roundoff. Lines 50-71 give exact rank-2 equality witness with `lhs=rhs=2`. Lines 73-76 state no violation found. | Equality witness `C=(1/sqrt(2))(|0,0><0,0|-|1,1><1,1|)`; diagnostic `gap=lhs-rhs`; run command. | Not a proof. Equality witness blocks any stronger constant in this formulation. | Next loop should verify any proposed strengthening against this witness before proof work. |
| `problem_statement_aristotle/co_mathematician/scripts/check_partial_trace_ineq.py` | Numerical checker for PT over random rank-one/rank-two matrices and local optimization over `C=UV*`. | Formalized as executable diagnostic; numerical only, not proof. | Docstring states “This script is not a proof” (lines 2-14). `partial_traces` uses tensor reshape and `einsum` (lines 47-52). `gap` implements `lhs-rhs` (lines 55-60). Random/optimized rank-factorized search appears in lines 78-115. I ran `python3 -m py_compile ...` and a small no-opt sample; it compiled and produced negative gaps for sampled rank 1 and rank 2 cases. | Useful regression/diagnostic harness; factorization `U @ V.conj().T`; partial trace implementation. | Optimization can only find counterexamples/evidence, not prove nonpositivity. Also local optimization depends on SciPy when not using `--no-opt`. | Add deterministic equality-witness unit test and tests for known false strengthened variants; keep random search as adversarial screen. |
| `problem_statement_aristotle/RequestProject/KroneckerSVBound.lean` | Lean formalization includes Frobenius norm identity and main Kronecker-sum SV theorem statement. | Partly formalized; main theorem incomplete; prose overstates. | Relevant theorem names: `frobenius_norm_kronecker_sum` (lines 92-114) has no `sorry` in inspected file; `kronecker_sum_sv_bound` (lines 128-139) ends with `sorry`. Repository search found one Lean `sorry`, no `admit`, `axiom`, or `unsafe` in Lean files. Module docstring says “The answer ... YES” (line 10) and uses false normal-density route (lines 37-39). | Definitions `frobeniusSq`, `vecNormSq`, `vecInner`, `kroneckerSum`; theorem `frobenius_norm_kronecker_sum`. | Main formal theorem not proved. Prose normal-density argument is marked false by coordinator report and should be treated as obsolete/overstated. | Formalize stable algebraic bridge lemmas next: projection formula/centering identity or equality witness, not `kronecker_sum_sv_bound` directly. |
| `research_harness/claim_cards/CLAIM-0001-rank-two-partial-trace.md` | Records PT as conjectural, with assumptions, non-assumptions, evidence, rejected routes, and promotion gates. | Current authoritative fail-closed claim card. | Lines 3 and 78-86 say conjectural/no accepted proof and list rejected routes. Lines 50-66 record sharp equality witness. Lines 97-105 set promotion gates. | Non-assumption checklist and promotion gates are reusable for adversarial review. | Correctly warns not to assume Hermitian/normal/positive/convexity. | Use as loop input template; update only after proof survives skeptic review. |
| `research_harness/problem_briefs/01_rank_two_partial_trace_inequality.md` | Problem brief restating PT, diagnostic gap, equality witness, non-assumptions, and attack surfaces. | Reusable problem brief; no proof claimed. | Lines 25-33 give equality witness; lines 35-45 give forbidden assumptions; lines 75-83 define promotion criteria. | Useful parametrizations: `C=UV*`, SVD form `C=s1|x1><y1|+s2|x2><y2|`. | None beyond “not a proof.” | Use as starting brief for next proof-search loop. |

## Lean/formalization audit

- Lean files found: `problem_statement_aristotle/RequestProject/KroneckerSVBound.lean`, `problem_statement_aristotle/RequestProject/Main.lean`.
- Relevant theorem names inspected:
  - `frobenius_norm_kronecker_sum`: appears completed in the inspected source.
  - `kronecker_sum_sv_bound`: contains `sorry`.
- Search result: one Lean occurrence of `sorry`; no Lean occurrences of `admit`, `axiom`, or `unsafe` found by repository search.

## Verification performed

- Read all required listed artifacts.
- Searched for relevant Lean files and incomplete markers.
- Ran:
  - `python3 -m py_compile problem_statement_aristotle/co_mathematician/scripts/check_partial_trace_ineq.py`
  - `python3 problem_statement_aristotle/co_mathematician/scripts/check_partial_trace_ineq.py --samples 200 --no-opt --seed 20260509`
- The sampled diagnostic run returned negative gaps for both rank 1 and rank 2 samples; this is numerical evidence only.

## Bottom-line classification

- The Kronecker-sum SV theorem is not proved in this repository.
- The rank-two partial-trace inequality remains conjectural/fail-closed in the reliable harness artifacts.
- The strongest reusable mathematical artifact is the bridge/equivalence in `partial_trace_inequality_needed_for_sv_bound.tex`.
- The purported standalone proof in `trace_inequality/rank_two_partial_trace_proof.tex` is invalid due to confusing ordinary rank with operator-Schmidt rank and using a false scalar inequality.
- The Aristotle partial-trace proof is circular for the intended goal because it assumes the Kronecker-sum SV bound.

## Recommended next loop input

Attack CLAIM-0001 directly under ordinary matrix rank <= 2. Start from the valid projection/duality bridge, and use rank factorization `C=UV*` or SVD `C=s1|x1><y1|+s2|x2><y2|`. Any proposed proof must explicitly avoid hidden Hermitian/normal/positive/operator-Schmidt-rank assumptions and must pass the exact equality witness before promotion.
